spring:
  liquibase:
    change-log: classpath:db/changelog/changelog.xml
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
